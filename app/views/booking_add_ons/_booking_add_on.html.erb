<div id="<%= dom_id booking_add_on %>" class="w-full sm:w-auto my-5 space-y-5">
  <p class="font-medium text-gray-900"><%= booking_add_on.add_on.name %></p>
  <p class="text-sm text-gray-500">Booking #<%= booking_add_on.booking_id %> &middot; <%= number_to_currency(booking_add_on.add_on.price) %></p>
</div>
